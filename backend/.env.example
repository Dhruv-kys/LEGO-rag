# 🧠 LEGO RAG — backend environment
# Copy to `.env` and fill in. Never commit the real .env.

# LLM brick (defaults to Claude — claude-opus-4-8 / claude-sonnet-4-6)
ANTHROPIC_API_KEY=

# Optional providers for other bricks
OPENAI_API_KEY=
COHERE_API_KEY=

# Server
PORT=8000
